# Contributing

The project will use a feature-branch workflow where each feature, bug fix, or improvement is developed in a separate branch. Changes will be submitted through pull requests instead of being pushed directly to the `main` branch. Pull requests must be reviewed and approved before they are merged. Branches should use descriptive names such as `feature/student-management` or `fix/grade-calculation`.
